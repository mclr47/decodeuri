import { HashSpyDirective } from './hash-spy.directive';

describe('HashSpyDirective', () => {
  it('should create an instance', () => {
    const directive =  HashSpyDirective;
    expect(directive).toBeTruthy();
  });
});
