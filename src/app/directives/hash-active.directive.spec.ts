import { HashActiveDirective } from './hash-active.directive';

describe('HashActiveDirective', () => {
  it('should create an instance', () => {
    const directive =  HashActiveDirective;
    expect(directive).toBeTruthy();
  });
});
